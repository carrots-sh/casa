version: 2

project_name: casa

before:
  hooks:
    - go mod tidy

builds:
  - id: casa
    main: ./cmd/casa
    binary: casa
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      - -X main.commit={{ .ShortCommit }}

archives:
  - id: casa
    ids: [casa]
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

nfpms:
  - id: casa
    ids: [casa]
    package_name: casa
    homepage: https://github.com/carrots-sh/casa
    maintainer: carrots-sh <hi@carrots.sh>
    description: Machine manager — files, tools, and secrets from one git repo. Push and pull your machines.
    license: MIT
    formats: [deb, rpm]
    bindir: /usr/bin

brews:
  - name: casa
    ids: [casa]
    repository:
      owner: carrots-sh
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: https://github.com/carrots-sh/casa
    description: Machine manager — files, tools, and secrets from one git repo. Push and pull your machines.
    license: MIT
    dependencies:
      - name: chezmoi
      - name: age # key restore runs before the packages script could ever install it
      - name: git
        type: optional
    install: |
      bin.install "casa"
    test: |
      assert_match "casa version", shell_output("#{bin}/casa --version")
    caveats: |
      make any machine feel like home: run `casa` to set up this machine from
      your repo, or `casa machine setup <github-user>` to point it somewhere.

checksum:
  name_template: "checksums.txt"

release:
  github:
    owner: carrots-sh
    name: casa
  prerelease: false
  name_template: "{{ .Tag }}"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
